# Sistema de Controle Contábil

Este é um sistema web moderno desenvolvido para facilitar a organização e controle de dados contábeis. A aplicação permite o upload de planilhas (Excel/CSV), edição de dados em tempo real, visualização de KPIs e gerenciamento de responsáveis.

## 🚀 Como Executar o Projeto

Siga os passos abaixo para baixar, instalar e rodar a aplicação em sua máquina local.

### Pré-requisitos
-   **Node.js**: Certifique-se de ter o Node.js instalado (versão 18 ou superior recomendada).
-   **Git**: Para clonar o repositório.

### Passo a Passo

1.  **Clone o repositório**:
    ```bash
    git clone https://github.com/rodrigod3v/.conti.git
    cd .conti
    ```

2.  **Instale as dependências**:
    ```bash
    npm install
    ```

3.  **Execute o servidor de desenvolvimento**:
    ```bash
    npm run dev
    ```

4.  **Acesse a aplicação**:
    Abra seu navegador e vá para `http://localhost:3000`.

## 🛠️ Comandos Disponíveis

-   `npm run dev`: Inicia o servidor de desenvolvimento.
-   `npm run build`: Cria a build de produção otimizada.
-   `npm start`: Inicia o servidor de produção (após o build).
-   `npm run lint`: Executa a verificação de código (linting).

## 📄 Documentação Técnica

Para detalhes sobre a arquitetura, tecnologias utilizadas e estrutura do projeto, consulte o arquivo [IMPLEMENTATION.md](IMPLEMENTATION.md).
